@{
    <!DOCTYPE html>
    <html>
    <head>
        <!-- head content -->
    </head>
    <body>
        <div>
            <div class="fixed-top">
            <!-- Include Header -->
                @Html.Partial("_Header")
            </div>

            <!-- common footer content -->
            <div class="text-body fixed-bottom">
                @Html.Partial("_Footer") <!-- Include Footer -->
            </div>
        </div>
    </body>
    </html>
}