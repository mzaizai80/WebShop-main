@model Product

<h2>Edit Product</h2>

<form asp-action="Edit" asp-route-id="@Model.Id" method="post">
    <!-- Add form fields for product editing -->
    <input asp-for="Name" />
    <!-- Add other fields as needed -->
    <button type="submit">Save Changes</button>
</form>