@using Microsoft.AspNetCore.Mvc.TagHelpers
@model WebShop.ViewModels.HomeViewModel

    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-5">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <h1>Welcome to the WebShop</h1>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                3Categories
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                
                                @if (Model != null && Model.Categories != null)
                                {
                                    @foreach (var category in Model.Categories)
                                    {
                                        <a class="dropdown-item" asp-controller="Home" asp-action="ProductsByCategory" asp-route-categoryId="@category.Id">@category.Name</a>
                                    }
                                }
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>


@* 
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model WebShop.ViewModels.CategoryViewModel

    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-5">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <h1>Welcome to the WebShop</h1>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                3Categories
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item" asp-controller="Home" asp-action="ProductsByCategory" asp-route-categoryId="@Model.Id">@Model.Name</a>
                                @if (Model.Subcategories != null && Model.Subcategories.Any())
                                {
                                    <div class="dropdown-submenu">
                                        <a class="dropdown-item dropdown-toggle" href="#">Subcategories</a>
                                        <div class="dropdown-menu">
                                            @foreach (var subcategory in Model.Subcategories)
                                            {
                                                <partial name="_CategoryDropdown" model="subcategory" />
                                            }
                                        </div>
                                    </div>
                                }
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

 *@

@*
<li class="nav-item">
<a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
</li>
<li class="nav-item">
<a class="nav-link text-dark" asp-area="" asp-controller="Category" asp-action="Index">1Categories</a>
<div class="dropdown-menu" aria-labelledby="navbarDropdown">
@if (Model != null && Model.Categories != null)
{
@foreach (var category in Model.Categories)
{
<a class="dropdown-item" asp-controller="Home" asp-action="ProductsByCategory"
asp-route-categoryId="@category.Id">@category.Name</a>
}
}
</div>
</li>
*@